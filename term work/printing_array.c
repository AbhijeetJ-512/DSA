#include <stdio.h>
#include <time.h>
void read_array(int a[], int n)
{
    for (int i = 0; i < n; i++)
    {
    }
}

void display(int a[], int n)
{
}

int main()
{
    int n;
    printf("Enter the number of elements needed in array:\n");
    scanf("%d ", &n);
    int a[n];
    read_array(a, n);
    display(a, n);
}